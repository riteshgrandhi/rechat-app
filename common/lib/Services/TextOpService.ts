// import { IOpSequence, OpType } from "../Entities/Entities";

export class TextOpService {
  // performOpsOnText(text: string, ops: IOpSequence): string {
  //   var updatedText = text;
  //   console.log(ops);
  //   ops.opList.forEach(op => {
  //     if (op.type == OpType.ADD) {
  //       updatedText =
  //         updatedText.substring(0, op.position) +
  //         op.text +
  //         updatedText.substring(op.position);
  //     } else if (op.type == OpType.DELETE) {
  //       updatedText =
  //         updatedText.substring(0, op.position) +
  //         updatedText.substring(op.position + op.text.length);
  //     }
  //   });
  //   console.log("text: " + text);
  //   console.log("updatedtext: " + updatedText);
  //   return updatedText;
  // }
}